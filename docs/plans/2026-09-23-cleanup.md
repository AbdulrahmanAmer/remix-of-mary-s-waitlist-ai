# Cleanup plan - 2026-09-23

Goal: every programmatic gap in `docs/audit/GAP-REGISTER.md` that does not depend on an open decision
(D2-D5 in `PROJECT-STATE.md`) is fixed, and the repo has gates that keep it clean.
Exit for the whole plan: `bun run typecheck`, `bun run lint`, `bun run test`, `bun run build` all exit 0,
and render-gate on `/` passes.

## Slices (in order; each is verified when it lands)

| # | Slice | Register IDs | Exit check |
|---|---|---|---|
| S1 | Test harness: vitest, `test` + `typecheck` scripts; unit tests for pure logic. Failing tests first for the two grounding bugs, then fix. | OP-02, VA-10, AI-05, AI-06 | new tests red before fix, green after; `bun run test` exit 0 |
| S2 | Hygiene: prettier error; delete unused shadcn components and deps they alone used; `inputValidator` -> `validator`; guard `ResizeObserver`; one echo-cleaning helper; declare `pendingFinish` before use. | LINT, FE-03, OP-01, AI-09 (deprecation), FE-09, VA-07, VA-08 | typecheck + lint + build; dev server shows no deprecation warning |
| S3 | API guard: same-origin check, body-size cap, per-IP rate limit on `/api/*` POSTs; length caps on turn input and speech text; stop echoing upstream errors. | AI-01, AI-02, AI-07, LD-02, AI-10 | curl probes: cross-origin 403, oversize 413, burst 429, same-origin passes |
| S4 | Behaviour bugs: surface TTS failure instead of silent success; clear intro timers; reflect idempotency guard; `aria-live` on the conversation; release audio output on unmount. | VA-01, FE-06, FE-05, FE-01, VA-04 | typecheck/lint/build; render-gate; code read of each path |
| S5 | Docs: roadmap claims corrected; playbook vs prompt conflict resolved; real README. | DR-01, DR-10, DR-04, OP-05/DR-07 | read-through |
| S6 | CI: GitHub Actions running typecheck, lint, test, build. | OP-02 | workflow parses; the same commands pass locally |
| S7 | Close-out: full gates, render-gate, re-index graph + vectors, update register + POSITION. | - | all gates exit 0 |

## Out of scope (needs an operator decision or a real device)

- D2 leads storage: LD-01, LD-03 (secret default), LD-06, LD-07.
- D3 provider seam: AI-08, DR-03.
- D4 beyond S3: signed session tokens that would close AI-03, AI-04, LD-05 (client-controlled state and the reflect injection path).
- D5 owner view: FE-02.
- Needs a real phone to verify: VA-02, VA-09, DR-10's audio behaviour, VA-03 (dropping the ScriptProcessor fallback).
- Large refactors with no UI/audio test cover yet: FE-04, VA-05 (the two god files). The S1 harness is the precondition.
- LD-04 privacy notice and retention: needs a policy, not code.
