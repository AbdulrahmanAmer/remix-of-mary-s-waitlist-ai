{
  "UX-01": {
    "refuted": false,
    "corrected_severity": "P0",
    "reasoning": "The finding holds, and P0 is the right severity. I made 0 live AI calls and 0 POSTs. My only live request was one GET to /api/lead, which is not an AI route.\n\nWhat I confirmed:\n1. **Live status, rechecked.** `curl https://omnisuite.omnikom.io/api/lead` at 2026-09-26 04:06:25Z still returns `{\"configured\":false}`. The leads lens got the same result at 03:10:39Z.\n2. **The cited lines match origin/main (44318ba), which the live site runs.** In `git show origin/main:src/routes/api/lead.ts`:\n   - line 20 returns configured:false;\n   - lines 53-72 only write to the sheet `if (result.configured)`;\n   - there is no log line or other store.\n3. **No other server-side copy exists on main.** A git grep of src/routes and src/lib found no other store: no Supabase, KV/D1 or email path. The only server log touching leads is `turn.ts:79`, and it logs field names only.\n4. **The client falls back to the browser only.** On main, `lead-lifecycle.ts:124-125` maps `!synced.configured` to `sync:\"local\"`. `waitlist-store.ts:9` stores under the localStorage key `omnisuite.waitlist.v1`. `waitlist-vault.tsx:94` shows \"entries stay on each visitor's device\".\n5. **The live bundle has the same code.** The leads lens's copy (`scratchpad/ux-audit/leads/live-routes.js`) contains those same strings.\n6. **The pending setup is documented.** `PROJECT-STATE.md:64` says the Apps Script is deployed and setting SHEETS_WEBAPP_URL/SECRET is pending. `.claude/POSITION.md:42-44` says \"conference is TODAY\" (2026-09-23).\n7. **It is worse than a silent failure on main.** The leads lens's mocked run (`leads/unconfigured.json`) shows that with configured:false the end screen still says \"EARLY ACCESS CONFIRMED ... position #149\". That position comes only from the local count.\n\nCorrections to the finding (none of them lowers the severity):\n- **\"Not even logged\" is only true on main.** On the PR #2 branch, `src/routes/api/lead.ts:99-104` now `console.warn`s a contact summary when a real lead is not saved (\"no sheet configured\"). That branch also adds a retrying outbox (`lead-lifecycle.ts:273,365`). The live site does not run this yet. It also only helps if Lovable keeps Worker logs where the operator can read them, which I could not check.\n- **\"The owner doesn't know it\" is overstated.** This is already registered as a P0: GAP LD-01 in PROJECT-STATE.md D2 (lines 19-24), with the same fix as option (a). The problem is an unfinished operator action, not a hidden defect. `docs/audit/GAP-REGISTER.md` is not in this clone (it is excluded locally), so I cited PROJECT-STATE instead.\n- **\"Every conversation since launch\" is plausible but not measurable.** The same log (PROJECT-STATE.md:66) says the live AI routes were failing from about 08:00 UTC on 2026-09-23, and the iPhone had no audio until PR #1. So the conference may have produced few completed conversations. Whatever it did produce is lost to the owner.\n- **The ITP point is minor.** The owner could never reach a visitor's phone anyway, and a booth device used daily keeps getting interaction. The cited post is also from 2020, not recent.\n- **The Retell label should be \"changes it\", not simply \"must_carry\".** With no sheet, Retell still keeps a server-side copy:\n  - `save_lead` patches the lead into the call's metadata (`src/lib/retell-handlers.server.ts:208-235, 384-406`);\n  - Retell keeps the transcript and post-call analysis;\n  - `docs/architecture/retell-migration.md:104-105` and `:222` say \"the row exists only in Retell's call history\".\n\n  So the operator could still find leads in the Retell dashboard or Get Call API, until the per-agent retention setting deletes them (`leads/retell_accounts_data-retention.md`: 3 days, 7 days, 30 days and more). The sheet (D2) is still needed for the owner view, positions and a durable export. It remains a sensible Retell go-live gate.\n\nI did not rerun the browser harness. The leads lens's mocked run plus the code and the live GET cover the claim, and the claim is about configuration and code paths rather than timing-sensitive UI behaviour.",
    "better_fix": "1. **Today:** set SHEETS_WEBAPP_URL and a non-empty SHEETS_WEBAPP_SECRET in Lovable (the deployed script rejects a wrong secret), then Publish -> Update. Confirm GET /api/lead returns configured:true, reachable:true and a leads count.\n2. **Merge PR #2's lead changes to main.** The live site gets the server-side log line (lead.ts:99-104) and the honest end screen, so it stops showing \"EARLY ACCESS CONFIRMED #149\" while sync is 'local'.\n3. **Add a durable server-side fallback** (KV/D1 or an email to the operator) for when the sheet is off or fails, since Worker console logs on Lovable may not be kept.\n4. **Add a CI or uptime check** that fails when production returns configured:false.\n5. **For Retell:** set the agent's retention to 30 days or more, and treat D2 as a go-live gate. Without a sheet, leads survive only in Retell's call history."
  },
  "UX-02": {
    "refuted": false,
    "corrected_severity": "P0",
    "reasoning": "The finding holds for the live site. On PR #2's current head, most of it is already fixed. I made no live AI calls and no POSTs. The only requests to the live site were GETs for the page, its JS bundles, /api/lead and /api/voice.\n\n**On origin/main, which the live site runs: confirmed line for line**\n- `src/lib/waitlist-store.ts:122-126`: `positionFor` returns `128 + hash % 640`.\n- `waitlist-store.ts:134`: `if (!next.position) next.position = positionFor(next.email || id)`. The number is set at the first local save and never changes after that.\n- `src/features/mary/conversation/lead-lifecycle.ts:124-125`: when the sheet is not configured, the result is `{position: entry.position, sync: \"local\"}`.\n- `lead-lifecycle.ts:129`: when the sheet saves without a position, it falls back to `entry.position`.\n- `src/features/mary/ui/end-screen.tsx:83-101`: shows \"Early access position #N\" whenever `result.position` is set. Otherwise it shows \"Your details are saved. Your position comes with the confirmation.\", including after a 500.\n- `end-screen.tsx:71`: the sync block appears only for `signed_up`, so callbacks never show a sync state.\n- `src/features/mary/conversation/text.ts:65-93`: contains \"Early access confirmed\", \"Invitations go out in order of position\" and \"The team receives your request straight away\".\n- `src/lib/mary-prompt.server.ts:112`: the callback final turn says \"the request is with the team\".\n- `mary-prompt.server.ts:127`: the CLOSE turn says \"you're in\" and \"we'll be in touch\".\n- `src/features/mary/conversation/turn-runner.ts:180-184`: the close is spoken first. Only after a 1.3 s wait does `onFinish` run `finalize`, which is when the sheet gets the lead.\n- `docs/mary-voice.md:338`: \"Only say it's recorded after it actually saved.\"\n\n**Checked on the live site just now**\n- The page still loads `/assets/routes-DZfdjxLS.js`. That bundle contains `return 128+t%640` and `{outcome:t,position:i.position,sync:\\`local\\`}`.\n- `GET /api/lead` returns `{\"configured\":false}`.\n- On main, the `/api/lead` POST handler (`src/routes/api/lead.ts:53-72`) does not save, forward or even log the lead when the sheet is not configured.\n- The audit screenshots match: `reliability/s01-end-unconfigured.png` shows \"EARLY ACCESS CONFIRMED … #707\", and `s01-end-500.png` shows the \"saved\" line after a 500. `leads/again.json` shows the same email getting local positions 690 and then 250.\n\nSo today every visitor who finishes is told they are confirmed and given a made-up number, while their details reach nobody. P0 is right for the live site. It goes hand in hand with UX-01 (the sheet not being connected).\n\n**On PR #2's current head (406d586, pushed to origin)**\nThe fix commit b991dc7 landed at 03:58, after the audit screenshots were taken (02:26-03:23). The end-screen part of the finding is fixed there:\n- `positionFor` now only feeds `localPosition` in the payload (`lead-lifecycle.ts:166`).\n- `deliver()` (`lead-lifecycle.ts:328-366`) shows a position only when the sheet returned one or already gave one in this conversation. For local and failed results the position is null.\n- `leadOutcomeCopy` (`lead-lifecycle.ts:434-548`) covers each case:\n  - pending: \"One moment / Securing your place…\"\n  - local or failed: \"Almost there / Not on the list yet\", with Try again and an outbox that retries\n  - callbacks: \"Not with the team yet\"\n- `tests/unit/lead-outcome.test.ts:43-50` checks that the unconfigured case shows no `#\\d|confirmed|on the list,`.\n- The callback prompt is fixed at `mary-prompt.server.ts:181-182`, and there is an honesty rule at `:252`.\n- Restart reloads the page (`mary-app.tsx:829-833`), so no delivery state carries over between conversations.\n\n**Still wrong on PR #2**\n- **Spoken close before the save (P2).** The web CLOSE line is `closingLine()`: \"You're on the list, {name} — your invite goes to that email…\" (`mary-prompt.server.ts:85-88`, used at `:194` and `:213`). It is still spoken before `finalize` sends anything to the sheet (`turn-runner.ts:317-320`). With the sheet off, MARY says \"You're on the list\" and the screen then says \"Not on the list yet\". The screen is honest and offers a retry, so this drops to P2 once PR #2 merges.\n- **Leftover step text (P3).** When the sheet saves but returns no position, the steps still include \"Invitations go out in order of position\" (`text.ts:112-117`, used by `leadOutcomeCopy` at `:499-507`).\n\n**Retell**\nThe Retell path on the branch already gates what MARY says on the save result:\n- `retell/playbook-condensed.md:45-52`: \"Never say saved, recorded or 'with the team' before a tool returned saved true.\"\n- `src/lib/retell-lead.server.ts:487-496`: when the save fails, the agent is told not to give a number or say it is saved.\n- The Retell end screen uses the same `leadOutcomeCopy`, fed by `end.synced`.\n\nSo Retell solves the spoken part by design. This should stay as a regression check rather than new work.\n\nThe finding is accurate. The fix is to merge PR #2, connect the sheet (UX-01), and make the web close line depend on the save result.",
    "better_fix": "1. Merge PR #2 (head 406d586). It already removes the made-up number from the screen and adds honest copy with a retry. Until it merges, the live site keeps telling every visitor \"confirmed #N\".\n2. Connect the sheet (UX-01), since `/api/lead` still returns `{\"configured\":false}`.\n3. Fix what remains in MARY's voice. Either delay the CLOSE follow-up (`closingLine`, `mary-prompt.server.ts:85-88`) until `finalize` has a result, or change it to \"I'm adding you now — your invite goes to that email when early access opens.\" That way she never says \"You're on the list\" just before the screen says \"Not on the list yet\".\n4. Drop \"Invitations go out in order of position\" from the base steps when there is no position (`text.ts:115`)."
  },
  "UX-03": {
    "refuted": false,
    "corrected_severity": "P1",
    "reasoning": "The finding is correct for the live site (origin/main), but it is already fixed, with tests, on the checked-out PR #2 branch and in the Retell agent. What remains is merging PR #2, not writing the fix again.\n\n**origin/main (live site): the finding holds.** All the cited lines match main:\n- mary-prompt.server.ts:90: requiredFields is every field except phone.\n- :119: DISCOVER says anyone who wants to go gets \"declined true, phase EXIT\".\n- :131-133: the gate says \"even if they ask you to finish now — in that case say you just need the last detail\". This is where \"To finish, what's the business?\" comes from in live-3.log.\n- :224-227: declined is set on leaving with no check for a spot.\n- :235: complete needs allCaptured.\n- text.ts:55: signed_up needs every field except phone.\n- text.ts:76-81: the declined screen says \"No spot reserved\".\n\nI reproduced it without any live calls. I exported main with git archive to scratchpad/ux-audit/skeptic-ux03/main and ran buildPrompt, finishTurn and endingFor (probe.ts, output in probe-main.out.json):\n- After Dana gives her name and email in rushed mode, the phase is still DISCOVER, with the gate \"Still missing: business, industry, operations ... even if they ask you to finish now\".\n- A model turn with intent leaving and declined true, holding name and email, gives finishTurn {complete:false, declined:true}, and endingFor returns \"declined\".\n\n**PR #2 branch (HEAD 406d586): already fixed.** Commit 35e5126 (\"name and email secure the spot\") names this exact scenario in its message. It is not on origin/main; it exists only on origin/claude/iphone-audio-playback-compat-lmufp8.\n- mary-prompt.server.ts:189-194: a fast lane for rushed visitors or those with no business goes EMAIL read-back, then CLOSE.\n- :215-219: the gate now needs name and email only, and says \"If they want to go, close warmly now with complete true\".\n- :246: the intent rule says the same for someone who wants off the call.\n- :315-331: in finishTurn, leavesWithSpot turns a leaver who has name and email into complete=true, declined=false. Only intent \"refusing\" still declines, which is by design.\n- text.ts:82: endingFor uses spotSecured (name and email).\n\nThe same probe on the branch export (probe-branch.out.json) shows:\n- The second turn's phase is CLOSE, with followUp \"You're on the list, Dana — your invite goes to that email…\".\n- The leaving turn gives finishTurn {complete:true, declined:false}, and endingFor returns \"signed_up\".\n\nUnit tests on the branch cover this: tests/unit/prompt-finish.test.ts:74 (\"someone leaving with name and email leaves signed up, not declined\"), prompt-phases.test.ts:82 (\"the fast lane\") and mary-text.test.ts:49.\n\nThe auditor's probe (endingFor with declined:true, complete:false and name plus email) still returns \"declined\" on the branch too. But the branch server never sends that combination for a leaver, so on the branch that probe does not show the bug.\n\n**Retell: already carried.**\n- retell/prompt-header.md:11 and :23: \"FAST LANE outranks discovery… Nobody who gave name and email leaves without a spot\" and \"a leaver with name and email gets the FAST LANE close\".\n- retell/playbook-condensed.md:47 and :84: save_lead with stage \"final\" \"straight from the fast lane with only name and email\".\n- src/lib/retell-lead.server.ts:209 and :273-275: SIGNUP_FIELDS is name and email, and the outcome is signed_up.\n- tests/unit/retell-lead.test.ts:179 covers it.\n\nSo the tag \"must_carry_into_retell\" is already met in code.\n\n**Severity: P1, not P0.** It is real on the live site today and loses exactly the rushed leads. But the core flow works for everyone else. Leads are not reaching a sheet yet either (SHEETS_WEBAPP_URL is unset per CLAUDE.md), so \"their row lands as declined\" only affects local state. And the complete fix is written, tested and waiting in PR #2.\n\nThe recommended code edits duplicate 35e5126 and should not be done again. One part of the recommendation is not in 35e5126: flagging a partial profile on the lead row. It is only partly there, because Retell's groundSaveLead lists missing fields in `rejected`. I did not check whether the web lead payload marks a partial profile.\n\nI made no live AI calls and started no servers. The repo was not modified; all probe files are under scratchpad/ux-audit/skeptic-ux03/.",
    "better_fix": "Merge PR #2: commit 35e5126 already makes name and email the spot, adds the fast-lane CLOSE and has finishTurn treat a leaver as signed up, and bfb87c7 brings the same rule into Retell. Before merging, make sure the tests prompt-finish.test.ts:74, prompt-phases.test.ts:82 and mary-text.test.ts:49 pass, and that the live smoke test covers the rushed path: a rushed visitor gives name and email, then 'did I get that right?', then the close, then the signed_up screen. The only possible follow-up is small: mark missing business, industry or operations on the web lead payload as a partial profile (Retell's groundSaveLead already lists them in `rejected`). Separately, keep an eye on the model labelling 'I have to go' as intent 'refusing', which would still give 'declined' when name and email are present (branch mary-prompt.server.ts:321)."
  },
  "UX-05": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is accurate for origin/main, which the live site runs. On the checked-out PR #2 branch, most of it is already fixed. No live AI calls were made (0 of 4 used) and no servers were started.\n\norigin/main (live): the finding holds, and is slightly worse than stated.\n- The phase chain is at mary-prompt.server.ts:118-125. REVEAL says \"Do not ask for anything in this turn\". LANES ends on \"one question that pulls them forward\". Only CONTACT gets the email.\n- docs/mary-voice.md:252 has the rule \"Never followed immediately by an ask\". The worked example at :437-468 asks for the email after 5 visitor answers.\n- DISCOVER on main also tells a visitor who clearly wants to go to switch to EXIT with declined true and no email ask (mary-prompt.server.ts:119). So a leaver during discovery is let go with no contact.\n- I ran main's real buildPrompt over the playbook's Sarah conversation (scratchpad/ux-audit/skeptic-ux05/sim.ts, source taken from main with git archive). The phases were WELCOME, DISCOVER x3, REVEAL, LANES, then CONTACT. The first email ask is on MARY's turn 7, after 6 visitor answers.\n- The cut-in claim is also true on main (turn-runner.ts:167-170): a cut-off reveal is replayed, and on main the reveal carries no ask.\n\nChecked-out branch (PR #2, commit 35e5126 \"MARY asks at the peak\"): the recommendation is already implemented, almost word for word.\n- The REVEAL followUp is \"That's the whole sign-up, by the way — where should your invite go?\" (mary-prompt.server.ts:198-205).\n- LANES comes after it, its followUp is the next housekeeping step, and it is skipped when rushed (:206).\n- A SPOT lane asks for the email right after the name when the visitor is rushed, has no business, or is leaving (:190-194, intentRule :246).\n- The \"Never followed immediately by an ask\" rule is gone from docs/mary-voice.md (§5 now puts the email ask in the reveal turn).\n- A unit test already pins the order: tests/unit/prompt-phases.test.ts:147-161 (\"the reveal's follow-up is the email ask\") and :249 (LANES skipped when rushed).\n- The same simulation against the branch gives WELCOME, DISCOVER x3, then REVEAL with the email ask: MARY's turn 5, after 4 answers. Rushed straight after the name goes directly to the SPOT email ask.\n- The cut-in point is mostly moot on the branch. A replayed REVEAL asks for the email again. A lanes beat that was cut off still counts through readState's lanesAttempted (:119-121).\n- Retell already carries the fix: retell/playbook-condensed.md:48-49 (FAST LANE, and REVEAL asks for the email in the same turn). retell/playbook-condensed.test-notes.md:46 lists \"Email asked last (P1)\" as addressed.\n\nEvidence problems:\n- Every line citation is for main only.\n- lead-lifecycle.ts:219-235 is wrong on both refs. Main's file is 182 lines, with partial rows at :88-92 and :159-160. On the branch those lines are deliver/resync code; the partial rows are at :207-208 and :326-334.\n- leave.json comes from a mocked /api/turn whose scripted MARY asks for the email only on turn 4 (leads-e2e.mjs:14-19). It shows that early leavers produce rows with no email. It does not show MARY's real phase order.\n\nWhat is still open on the branch:\n- A neutral visitor who drops during discovery (answers 1-3) without signalling that they're in a hurry or leaving still leaves no email. DISCOVER (:196) asks for it only if they seem ready to go.\n- There is no always-visible email field. The typed form (lead-fallback.tsx) appears only on the fallback stage.\n\nSeverity: P1 describes the live site today. But the fix is already written, tested and waiting in PR #2, and what remains is a narrower tradeoff against the reveal's \"no form\" premise. For the audit's to-do list that is P2.\n\nRetell: the order is already carried in the Retell prompt. The discovery-drop gap remains there too, because step 3 DISCOVER still comes before the email for neutral visitors.",
    "better_fix": "1. Merge PR #2 (commit 35e5126). It already asks for the email at the reveal and asks rushed or leaving visitors right after their name. It also skips LANES when they're rushed, pins the order in tests/unit/prompt-phases.test.ts, and carries the same order into retell/playbook-condensed.md. Don't re-implement any of that.\n2. Treat what remains as a measured decision, not a rewrite. Once PR #2 is live, count the in_progress/abandoned rows that have a name but no email, grouped by how many turns the visitor gave. Only if many drops happen at answers 1-3, add an email ask after the name for neutral visitors, or show an always-visible \"leave your email\" field.\n3. Fix the evidence line references to the branch: mary-prompt.server.ts:190-216, lead-lifecycle.ts:207-208 and :326-334."
  },
  "UX-04": {
    "refuted": false,
    "corrected_severity": "P1",
    "reasoning": "The finding is accurate for origin/main (44318ba), which is what the live site runs. It is mostly fixed on the checked-out PR #2 branch (HEAD 406d586, commit f511644), but that fix has not been merged or published. So I am not refuting it, and I lowered the severity from P0 to P1: the problem only appears during an upstream outage, and the code that fixes it is already written, unit-tested and waiting on merge and Publish.\n\n**origin/main and the live site (confirmed)**\n- I fetched https://omnisuite.omnikom.io/. It still serves `assets/routes-DZfdjxLS.js`. That file contains \"lost my train of thought\" and does not contain \"line is down\", so the fallback form is not live.\n- `git show origin/main` matches the cited lines:\n  - `mary-stream.ts:126-128`: an `{\"type\":\"error\"}` line becomes the \"Sorry — I lost my train of thought\" turn.\n  - `turn-runner.ts:34,185-186`: every failure says SNAG again. There is no failure counter and no fallback.\n  - On main the welcome also goes through /api/turn, so the outage breaks the opener too.\n- Main has no `lead-fallback.tsx`.\n- Why typed answers get no reply on main: the fallback line nearly repeats the welcome, so it is suppressed (`turn-runner.ts` main lines 129-152). The retry through the server function returns 500, so nothing is said. The lens screenshot `s02-error-line-1-welcome.png` does show the caption cut at \"Could you say\".\n- Two citations are imprecise:\n  - `audio-engine.ts:2171` is HEAD's numbering. On main the same `if (!res.ok) return \"\"` is at line 2143.\n  - \"Typed details are never captured\" is overstated. The abandoned beacon carries the full transcript (main `lead-lifecycle.ts:65`, `transcriptOf`), so the typed email reaches the sheet's transcript column. It is only missing from the structured name and email fields. Separately, `SHEETS_WEBAPP_URL` is unset, so nothing reaches a sheet anyway.\n\n**Checked-out branch (HEAD 406d586): most of the recommendation is already built**\n- `turn-runner.ts:78,200-213`: TurnRunner counts consecutive failures. `text.ts:194` sets `FALLBACK_AFTER_FAILURES = 2`. After that the runner dispatches ENTER_FALLBACK.\n- `ui/lead-fallback.tsx` is the form the finding asks for: \"MARY's line is down\", name, email and optional business. It submits through `lead.finalize` to `/api/lead` (`mary-app.tsx:840-851`).\n- `mary-stream.ts:180-184`: an error line now throws `TurnFailedError`.\n- The opener is fixed text and makes no AI call (`mary-app.tsx:184-187`).\n- The hold path uses `transcribeHeldAudio` (`hold-talk.ts:37-61`), which throws `TranscribeUnavailableError`. The user sees `TRANSCRIBE_FAILED_LINE` (\"I couldn't process that just now — hold the button and say it again, or type it below\"), not \"speak close to the phone\" (`mary-app.tsx:266-270`).\n- Unit tests cover this: `tests/unit/turn-runner.test.ts:209-222`, `hold-talk.test.ts` and `session-reducer.test.ts`.\n\n**My replay on HEAD (local dev on 5173, all routes mocked, 0 live AI calls, server stopped afterwards)**\nFiles are in `scratchpad/ux-audit/skeptic-ux04/`: `error-line.json`, `502.json` and screenshots `error-line-1..6.png`, `502-1..6.png`. In both variants the sequence was:\n1. The fixed opener appears on screen, with \"My voice didn't come through\" (speech 502).\n2. Each hold gets \"I couldn't process that just now…\".\n3. The first typed answer gets \"I hit a snag on my side\".\n4. The second typed answer switches to the fallback form (t≈29 s), with the email pre-filled from the transcript.\n5. Submitting POSTs `/api/lead` with `outcome: signed_up`, `name: Sam Rivera`, `email: sam@example.com`.\n6. The end screen then shows \"position #42\" when the sheet saves, or an honest \"Not on the list yet… safe in this browser\" when it is unconfigured.\n\n**What is still open on HEAD (P2-level)**\n- There is no `/api/health` and no outage check before Start.\n- Failed transcriptions do not count toward the fallback. A voice-only visitor who never types keeps seeing the same (deduplicated) \"type it below\" notice and never reaches the form.\n- Hands-free mode still uses the old `transcribe()` (`voice-line.ts:360`, `audio-engine.ts:2171`), so a 502 there is silently treated as no speech.\n- The name is not pre-filled from typed text, and the grey placeholder \"Sam Rivera\" looks like a filled-in value (`lead-fallback.tsx:145`; see `error-line-5-typed2.png`).\n- There is no alerting.\n\n**Retell**\n- Partly carried over. If a Retell call never goes live, `retell-call.ts:537-559` hands the queued text back to TurnRunner (`mary-app.tsx:644-648`), which reaches the form after 2 failures if Lovable AI is also down.\n- A Retell failure after the call is live goes to `onEnd` (`retell-call.ts:490-500`), not to the form.\n- An outage of Retell's own LLM in the middle of a call is invisible to the client.\n\nRetell changes this finding rather than solving it.",
    "better_fix": "Ship first: merge PR #2 (f511644 plus the later V1 commits), then have the operator press Publish -> Update in Lovable and confirm the live routes bundle contains \"line is down\". After that, close these gaps:\n1. Count TranscribeUnavailableError toward the same failure counter. For example, 2 transcription failures in a row open LeadFallback, or at least show a \"Leave details instead\" button.\n2. Route hands-free `voice-line.ts:360` through `transcribeHeldAudio` so a 502 there shows `TRANSCRIBE_FAILED_LINE`.\n3. Pre-fill the name from typed lines, and change the placeholder so it doesn't look like a real value.\n4. Add a cheap GET /api/health that the landing reads before Start, so it can offer \"leave details\" up front.\n5. Send a Retell post-live error or unexpected end with nothing collected to LeadFallback, not to the end screen.\n6. Alert on turn/speech/transcribe 5xx (UX-35)."
  },
  "UX-06": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is accurate for origin/main, which is what the live site runs. It is mostly out of date for PR #2: the branch has moved since the evidence was captured. The lead500, schema-probe and s01 files date from 03:07–03:16 UTC, and the outbox fix landed in b991dc7 at 03:58 plus 783f37c at about 04:15. I made no live AI calls and sent only one GET to live /api/lead, which returned {\"configured\":false}.\n\n**origin/main (live): confirmed.** Every citation matches.\n- lead-lifecycle.ts:100 sets finished=true, and :116-133 sends a single syncLead.\n- flush() returns early at :146, and the checkpoint is a pure debounce at :88-93.\n- lead-sync.ts:77 has the 9 s client timeout, lead.ts:63 the 9 s sheet timeout, api-guard.ts:24 the 20/min limit, and Code.gs:127 waitLock(25000).\n- End-screen.tsx:100 shows \"Your details are saved. Your position comes with the confirmation\" whenever there is no position, including sync=failed.\n\nI reproduced this on a git-archive of main with the harness (iPhone mode, /api/lead mocked to 429; scratchpad/ux-audit/skeptic-ux06/main-final-429.json, main-end-0s.png):\n- There was exactly one POST, the final one at 24.4 s, which got a 429.\n- The screen said \"Your details are saved\".\n- Nothing was retried over 45 s open, after a hidden-to-visible toggle, or on a revisit. No beacon was sent on close, and no outbox exists.\n\n**Branch HEAD 783f37c: the headline claims are false.** \"Sent once and never retried\" and \"still told it saved\" no longer hold.\n- deliver() stashes the row in a localStorage outbox keyed by sessionId before each attempt, and drops it only when the sheet says saved:true (lead-lifecycle.ts:351-389).\n- Failures back off 20 s, 40 s and so on, capped at 1 h (lead-sync.ts:171-174).\n- Retries run on page open, online and visible (lead-sync.ts:350-362), and on \"Try again\" or a correction.\n- The copy is honest: \"Not on the list yet\", \"Try again\".\n\nSame harness run on the branch (branch-final-429.json, branch-end-0s.png):\n- The final POST at 25.9 s got a 429, the row was queued (attempts 1), and the screen said \"Almost there… Not on the list yet · Try again\".\n- A hidden-to-visible toggle resent it at 73.3 s.\n- The server also now logs unsaved real leads (lead.ts isLead / console.warn). This does not help with a 429, which the guard rejects before the route runs.\n\n**Branch HEAD: residual gaps that still hold, measured.**\n1. There is no timed retry while the end screen stays open and visible. After 45 s idle there was still only 1 POST, although the row was due at 20 s. The watcher only fires once at load (+4 s), on online, or on visible.\n2. Closing the tab sends no beacon: beaconsOnClose=0. The pagehide listener exists only in the call and fallback stages (mary-app.tsx:544-556), and flush() still returns once finished (lead-lifecycle.ts:320). The row waits in localStorage until the visitor comes back in the same browser. For a conference visitor who scanned a QR code on their own phone (index.tsx:6, mary-voice.md:404), that may be never.\n3. The load-time flush respects the backoff (lead-sync.ts:308). A revisit 8 s later, with the limit cleared, sent nothing because nextAt was still 40 s away. Only the single +4 s trigger runs on load.\n4. The checkpoint is still a fire-and-forget pure debounce that never goes into the outbox (lead-lifecycle.ts:203-209).\n5. The /api/lead limit is still 20/min per IP (api-guard.ts:24). Code.gs:147 still waits up to 25 s for the lock, while the Worker and client both give up at 9 s (lead.ts:87, lead-sync.ts:79).\n\n**Overstatements in the finding.**\n- The 9 s timeout against the 25 s lock is not necessarily a loss. Apps Script keeps running after the Worker aborts, so the row can land after the client has recorded a failure: a false \"failed\", fixed by the idempotent retry.\n- \"Only one POST per conversation, so the email is lost\" comes from the mock's compressed pace of about 3.3 s per turn, which is under the 5 s debounce. Real voice turns are longer, so a checkpoint carrying the email will usually fire before the final one. The email is lost mainly on fast typed conversations. Commit 35e5126 also moves the email ask earlier (\"asks at the peak\").\n- The live sheet is not configured, so today no lead reaches a sheet at all. This retry gap is latent until SHEETS_WEBAPP_URL is set.\n\n**Severity.** P1 is right for main taken alone. The remaining work once PR #2 merges is P2.\n\n**Side bug found while reproducing (branch, P3).** resync() rebuilds the payload with now(), so every Try-again, visibility or correction resend makes durationSec longer. The measured outbox row showed durationSec=67 for a call that lasted about 20 s (lead-lifecycle.ts:181, 272).\n\n**Retell: changes it.** The voice path writes the row on the server (save-lead function and webhook). The webhook returns 502 on a sheet failure so that Retell retries (retell-handlers.server.ts:464, 492). Retell retries up to 3 times when it gets no 2xx within 10 s (https://docs.retellai.com/features/webhook-overview, fetched copy in scratchpad/ux-audit/leads/retell_features_webhook-overview.md:55). That traffic comes from Retell, not the visitor's IP, and has a budget of 600/min (api-guard.ts:28), so the shared-venue-IP 429 on saves goes away. The per-IP limits on /api/retell/web-call (20/min) and on /api/turn (30/min, which would break a venue conversation before the lead limit does) still apply. The typing path, the fallback form and the end screen still run through /api/lead and the outbox.\n\nAll my files are in /tmp/claude-0/-home-user-remix-of-mary-s-waitlist-ai/5db0fce8-f9f0-58ea-ab86-b04df5aa8cc5/scratchpad/ux-audit/skeptic-ux06/ (final-429.mjs, main/branch JSON and PNGs). The repo was not touched, and my dev servers on ports 5241 and 5242 are stopped.",
    "better_fix": "Most of the recommendation is already on PR #2 (b991dc7 and 783f37c); merge it, then close the remaining gaps:\n(a) While the end screen shows sync=failed and the page is visible, schedule a timed resync at the entry's nextAt, for example up to 3 automatic tries, so an idle, visible screen is not stuck waiting.\n(b) Add a pagehide/visibilitychange-to-hidden listener in the \"done\" stage that sends the queued row with sendBeacon when it is still unsaved, because a venue visitor may never come back to that browser.\n(c) On load, send an entry that has never succeeded once, ignoring its backoff, or re-run the outbox check when its nextAt arrives, instead of relying on the single +4 s trigger.\n(d) Turn the checkpoint into a throttle with a maximum wait of about 5-8 s, and stash it in the outbox once name, email or phone exist.\n(e) Key the /api/lead budget by IP plus sessionId, or raise it; review /api/turn's 30/min per IP too, since it breaks a venue conversation first.\n(f) Freeze durationSec at finalize so a resync does not make it longer.\n(g) Align the timeouts: use tryLock or waitLock of 8 s or less in Code.gs, or treat a Worker timeout as \"pending\" rather than \"failed\", because the row may still land."
  },
  "UX-07": {
    "refuted": true,
    "corrected_severity": "P3",
    "reasoning": "The bug is real on the live site (origin/main, 44318ba). But it is already fixed on the checked-out PR #2 branch (HEAD 783f37c) by commit f511644 \"fix(call): a call that never hangs…\" (03:54Z). The audit lenses collected their evidence before that commit: funnel 02:42, reliability 03:10, mobile 03:48. The finding also cites line numbers from the stale commit 5f531ad. I'm refuting it as \"already fixed\", not as wrong about live.\n\nORIGIN/MAIN (LIVE), CONFIRMED\n- Code:\n  - hold-recorder.ts calls getUserMedia at line 118 with no try/catch and no mediaDevices guard.\n  - micMessage maps anything that isn't a MicUnavailableError to the generic copy (text.ts:98-119).\n  - The hold effect runs recorder.open() after start() has already set the specific message (mary-app.tsx:213-225 and 464-467 at 44318ba).\n  - Hold mode renders no mic button (composer.tsx `{!holdMode && ...}`).\n  - The idle nudge says \"you can talk to me\" (text.ts:138).\n- Reproduced against https://omnisuite.omnikom.io with every /api route mocked (0 live AI calls, no lead POST). Script: scratchpad/ux-audit/skeptic-ux07/repro.mjs.\n  - ios-denied (NotAllowedError) and desktop-denied: the notice reads \"I couldn't open the microphone. Tap the mic button to try again…\". Hold is visible, micButton is null, and the status says \"Hold the button while you talk\". getUserMedia count went 2 → 3 → 4 across two hold presses with no visible change.\n  - inapp-nomedia (Instagram UA, mediaDevices undefined): the same generic copy, so the in-app copy is never shown.\n  - Screenshots: shots/ios-denied-live-main-after-holds.png, inapp-nomedia-live-main-after-holds.png, desktop-denied-live-main-after-holds.png.\n\nBRANCH HEAD (PR #2), ALREADY FIXED\n- hold-recorder.ts:25-38 maps error names to a reason (micFailureReason). Lines 139-143 throw unsupported/insecure when mediaDevices is missing. Lines 144-156 wrap getUserMedia with the comment \"A raw DOMException reads as 'unknown' upstairs\". That is recommendation (1).\n- mary-app.tsx:474-481: a mic error in hold mode sets voiceWanted=false and switches to typing mode. There the mic button (\"Turn the microphone on\") exists. That is recommendation (3) in its core form.\n- text.ts:134: the denied copy now says \"Allow it for this site in your browser settings, then tap the mic button\". text.ts:165-168: the idle nudges now point to typing or the mic.\n- Retell (recommendation 4) is already in place:\n  - start() primes the mic and sets micMessage when that fails (mary-app.tsx:705-713).\n  - micReady=false goes to failBeforeLive(null) (retell-call.ts:290, 536-560).\n  - Control returns to MARY's text chat and the specific notice is kept.\n- Reproduced against local dev on HEAD with the same mocks; the server has since been stopped.\n  - ios-denied and desktop-denied: the specific \"browser settings\" copy stays. Hold is gone, the mic button is visible, and the status reads \"Type your reply, MARY is reading.\"\n  - inapp-denied and inapp-nomedia: the in-app copy \"Tap the ⋯ menu and choose 'Open in browser'…\" stays.\n  - Screenshots: shots/*-local-head-after-welcome.png.\n\nWHAT'S LEFT ON HEAD (P3)\n- A second getUserMedia call still fires right after the tap (count=2 at 400 ms). The hold effect (mary-app.tsx:340-352) runs before the typing-fallback effect switches modes, and its result is then thrown away. Recommendation (2) is not done, but the user sees no effect.\n- After a hard deny, \"Turn the microphone on\" calls getUserMedia again (2 → 3) and nothing visible changes. In an in-app browser that button can never work but stays on screen. There are no per-browser steps.\n- No unit test covers HoldRecorder with a raw DOMException or the typing-fallback effect. The tests only use MicUnavailableError (text-reliability.test.ts:80-85, retell-call.test.ts:610-617).\n- I did not verify the external citations (the Chrome `<usermedia>` blog post, the Meta forum thread).\n\nSeverity: P1 for live users until PR #2 merges. The remaining gap on the branch is P3.",
    "better_fix": "No new build work is needed for the core bug: merge and deploy PR #2 (f511644) and live is fixed. Optional P3 follow-ups on the branch:\n(a) Skip the hold effect's recorder.open() when start()'s primeMicPermission already failed, e.g. with a c.micPrimeFailed flag read at mary-app.tsx:341. This also stops the second getUserMedia call.\n(b) After a denied or in-app failure, give \"Turn the microphone on\" visible feedback. Hide it inside in-app browsers, or show per-browser steps; the Chrome `<usermedia>` element only after feature detection.\n(c) Add unit tests for the HoldRecorder raw-DOMException mapping (a NotAllowedError from getUserMedia should give MicUnavailableError('denied'), and a missing mediaDevices should give 'unsupported') and for the hold-to-typing fallback.\nRetell: the adapter already hands a mic denial back to MARY's text chat with the specific notice kept, so carry the existing behaviour forward rather than building it again."
  },
  "UX-08": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is correct for the live site (origin/main 44318ba). Most of it is already fixed on the checked-out PR #2 branch (HEAD 783f37c), so I lowered it to P2 for the remaining work. I made zero live AI calls. Every route was mocked with the funnel lens's measured delays (turn 2980 ms, speech 1280 ms, 0.42 s per word).\n\n**origin/main (what the live site runs): the finding holds.**\n- `welcome()` calls `run()`, a full `/api/turn` call (turn-runner.ts:52-54 on main). That route uses forceReasoning (src/routes/api/turn.ts:49).\n- The WELCOME instruction mentions no waitlist, benefit or time (mary-prompt.server.ts:116).\n- DISCOVER says \"never mention the waitlist offer again\" (:119).\n- The landing preview is \"Want first access to OmniSuite through the launch waitlist?\" (landing.tsx:9).\n- The replay log agrees with the finding (scratchpad/ux-audit/funnel/log-voice-ios.json):\n  - tap at 7805 ms; `/api/turn` requested at +259 ms\n  - `/api/speech` requested at +3.26 s; first audible at +4.8 s\n  - first line ends at +14.0 s; follow-up requested at +14.25 s; follow-up audible at +15.8 s\n- One small citation slip: `mary-app.tsx:533` is from the old branch head 210a1ea (on main the call is at :474).\n\n**Branch HEAD 783f37c (pushed, PR #2): largely fixed.** Commits f511644 and 35e5126 landed today at 03:53-03:54 UTC, after the audit's evidence was gathered at about 02:30.\n- The welcome is now a fixed opener, spoken without a model call:\n  - turn-runner.ts:83-86 and :163-178 (`open()`)\n  - mary-app.tsx:184-187 picks from `OPENING_LINES` (src/lib/retell-shared.ts:38-42) or `welcomeBackLine`\n  - `/api/turn` is called only after the visitor's first answer\n- The opener text is the recommended one almost word for word (\"Two minutes with me and you're on the list. What should I call you?\").\n- The server WELCOME phase now says the fixed opener word for word (mary-prompt.server.ts:185, `OPENERS` at mary.functions.ts:40).\n- \"never mention the waitlist offer again\" is gone; grep finds no match on the branch.\n- Each opener is one line ending in the question, so the 1.8 s follow-up gap is gone.\n\n**I reproduced the branch in a scratch copy** (git archive of HEAD, dev server on port 5288, now stopped). I used the same funnel driver and mocks. Logs are in scratchpad/ux-audit/skeptic-ux08/voice-ios.out and voice-desktop.out.\n- iPhone emulation: `/api/speech` requested at +190 ms after the tap; first audible at +1694 ms. The opener finished at +13.5 s.\n- Desktop: `/api/speech` requested at +203 ms; first audible at +1758 ms. The opener finished at +12.2 s.\n- In both runs the status reads \"Hold to talk\" within about 0.25 s, not \"Thinking…\".\n\n**Still open on the branch (why P2 and not refuted):**\n1. There is no pre-rendered audio. `audio-engine.ts:1084` still sends every line, the opener included, to `/api/speech`, with no cache or prefetch, so each visit pays for TTS. First sound is therefore about the TTS first byte (1.28 s live) plus about 0.4 s, around 1.7 s, not under 1 s. A real iPhone adds the permission sheet, because `start()` awaits `primed` before START_CALL.\n2. The openers are 24-28 words, about 11-13 s at the measured pace, and the name question comes at the end. The question now starts around +11-12 s, down from +15.8 s. The content now says what they get and how long it takes.\n3. The landing preview (landing.tsx:13, \"Hi, I'm MARY, the AI concierge for OmniSuite. What should I call you?\") still matches none of the three `OPENING_LINES`.\n4. Two opener sets have drifted apart: `OPENERS` has 4 variants and serves as the server prompt's welcome, while `OPENING_LINES` has 3 variants and is what the client actually speaks.\n\n**Retell claims:**\n- The branch's Retell config already follows the recommendation. retell/llm.json:2-3 has `start_speaker` set to agent and `begin_message` set to \"{{opening_line}}\", a fixed line passed in as a variable, not LLM-generated.\n- Retell's API docs say the LLM generates the first message only when `begin_message` is not set (docs.retellai.com/api-references/create-retell-llm).\n- The 10 s minimum charge applies to \"dynamic opening messages\" (docs.retellai.com/accounts/billing-exceptions). Whether a message filled from a variable counts as dynamic is untested; retell-migration.md:218 already lists this as an open question.\n- `begin_message_delay_ms` exists, range 0-5000 (docs.retellai.com/api-references/create-agent), but retell/agent.json does not set it. That is a real remaining gap.\n- The 1250 ms first-packet figure in community.retellai.com thread 3660 was posted by a user (\"maxi\") on 2026-09-17, not by Retell staff. Staff member Shah-Fazal is the one who recommended 1000-1500 ms and tuning it to your p95. The recommendation stands; only the attribution is wrong.\n\n\"changes_with_retell\" is the right Retell label.\n\n**Severity:** P1 is accurate for the live site today. Merging PR #2 removes the model call, the jargon and the gap. What remains is the ~1.7 s TTS-bound start, the long single opener, the preview mismatch and the missing `begin_message_delay_ms`. That is P2.",
    "better_fix": "Merge PR #2 to fix the live site. For what remains:\n1. Pre-render the 3 `OPENING_LINES` (and the welcome-back line without a name) as static audio in `public/` and play them from the Start tap. That removes the ~1.3 s TTS first-byte wait and the per-visit TTS cost.\n2. Merge `OPENERS` (mary.functions.ts) and `OPENING_LINES` (retell-shared.ts) into one source.\n3. Shorten each opener to about 15 words and put the name question within about 6 s.\n4. Make landing.tsx:13 show a real opener.\n5. For Retell, add `begin_message_delay_ms` of about 1000-1500 to retell/agent.json, then measure clipping at p95. Also test whether \"{{opening_line}}\" is billed as a dynamic opener."
  },
  "UX-10": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is true for origin/main, which the live site runs. It is already fixed on the checked-out PR #2 branch (HEAD 783f37c, pushed, identical to origin/claude/iphone-audio-playback-compat-lmufp8). I made no live AI calls and no POSTs. My only live request was GET /api/lead.\n\n**Confirmed on origin/main (live):**\n- The playbook is the runtime system prompt: `import MARY_VOICE_SPEC from \"../../docs/mary-voice.md?raw\"` in main:src/lib/mary-prompt.server.ts:4.\n- main:docs/mary-voice.md:193, 459 and 509 say \"So I know where to send your early-access confirmation — best email?\".\n- main:docs/mary-voice.md:386-387 says \"Only to confirm your spot and send your launch details\".\n- main:src/lib/mary-prompt.server.ts:125 says \"Get their email as housekeeping tied to their spot confirmation\".\n- main:src/features/mary/ui/end-screen.tsx:100 says \"Your position comes with the confirmation.\"\n- `git grep MailApp|GmailApp|sendEmail|resend|sendgrid|postmark|smtp` on origin/main over src/ and docs/google-sheets finds nothing.\n- The live bundle https://omnisuite.omnikom.io/assets/routes-DZfdjxLS.js contains \"Your position comes with the confirmation.\" (saved at scratchpad/ux-audit/verify-ux10/).\n- Live GET /api/lead returns {\"configured\":false}. So on the live site the lead is not even stored, and the promised email is doubly impossible.\n\n**Already fixed on the PR #2 branch** (commits 35e5126 \"promises only what is true\" and b991dc7 \"optional confirmation email\"):\n- The prompt forbids the promise: src/lib/mary-prompt.server.ts:252 says \"Nothing here sends a confirmation email ... Never promise a confirmation email\". docs/mary-voice.md:398-402 says the same.\n- The ask became \"your invite goes there\" (mary-voice.md:447, 453).\n- Tests guard it: tests/unit/prompt-playbook.test.ts:31 and prompt-phases.test.ts:322 check `not.toMatch(/early-access confirmation/)`.\n- The finding's recommendation is already built. docs/google-sheets/Code.gs:246-251 calls `sendConfirmation` inside `upsertLead` after the row is written. Code.gs:303-338 sends through MailApp once per address, at most 2 per row, and the body includes name, details, position and \"reply to fix anything\". The README section \"Confirmation email (optional, off by default)\" gives the steps and quotas.\n- The emailed flag reaches the page: src/routes/api/lead.ts:91 sets `emailed`, and src/features/mary/conversation/lead-lifecycle.ts:467-469 shows \"A confirmation is on its way to X\" only when the sheet reports a send.\n\n**The Retell citation is stale:**\n- scratchpad/retell/plan.md:491 (\"team will confirm by email\") is an old scratch plan, not repo code.\n- The repo's handler, src/lib/retell-lead.server.ts:488, says \"Do not give a position number and do not say it is saved\".\n- retell/prompt-header.md:19 says \"Never promise a confirmation email\".\n- So the \"must_carry_into_retell\" part is already carried.\n\n**What is left once PR #2 merges** is a missed opportunity, no longer a trust breach:\n- (a) No email is ever sent until the operator sets SHEETS_WEBAPP_URL, pastes the new Code.gs, authorises MailApp and sets CONFIRMATION_EMAIL=on. It is off by default.\n- (b) Callbacks never get an email: Code.gs:249 sends only when the status is signed_up, although the finding suggests signed_up or callback.\n- (c) The end screen shows no \"check your inbox\" when email is off (correct), and there is no share link (UX-53).\n\nSo the finding is right about the live site today (P1 there until PR #2 merges). The remaining work is a merge plus operator config, which is P2 at most.",
    "better_fix": "Don't rewrite the prompt or build a mailer: merge PR #2. It already takes the confirmation-email promise out of the prompt, the playbook and the end screen, has tests guarding that, and adds an optional MailApp confirmation in Code.gs upsertLead. Then do the operator steps: set SHEETS_WEBAPP_URL in Lovable, paste the current Code.gs, run testConfirmationEmail to grant MailApp, set CONFIRMATION_EMAIL=on (plus CONFIRMATION_REPLY_TO), and redeploy. Check with ?action=ping for confirmationEmail:true. Optionally extend Code.gs:249 so callbacks get a short \"we'll call you\" note too. On a consumer Gmail account, watch the 100/day MailApp quota. Retell needs nothing extra: retell-lead.server.ts goes through the same Code.gs upsert, and retell/prompt-header.md:19 already forbids the promise. Drop the stale plan.md:491 line from any further Retell planning."
  },
  "UX-09": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is true of the live build (origin/main 44318ba). PR #2 (branch HEAD 783f37c, commit f511644 \"a call that never hangs…\") already fixes its main claim, a short tap. What is left on PR #2 is narrower: a brush of 300 ms or more that produces no words. So I'm confirming it but lowering it to P2. It stays P1 on the live site until PR #2 merges.\n\n**origin/main (live): confirmed.**\n- `mary-app.tsx:153-160`: `onPress` calls `runner.interrupt()` on press.\n- `mary-app.tsx:165`: `onCancel: waiting`, with no notice.\n- `turn-runner.ts:78-81`: interrupt.\n- `turn-runner.ts:127` and `146-148`: `SET_COLLECTED` only runs after the stale check.\n- `turn-runner.ts:155`: the follow-up is gated on `isHeld`.\n- `hold-talk.ts:4` and `123-124`: `MIN_HOLD_MS` = 300, and `onCancel` fires for a short hold.\n- `composer.tsx:94-98`: the button reads \"Hold to talk to MARY\" or \"Thinking… hold to talk\".\n- The funnel screenshot (`shots/tap-ios-after-tap-during-welcome.png`) shows the cut line \"Hello. I'm MARY, Omnikom's …\".\n- My own run against the live site used only mocks: every /api and /_serverFn route was fulfilled locally and sendBeacon was stubbed, so it made 0 live AI calls and wrote no lead. Script: `scratchpad/ux-audit/skeptic-ux09/repro-main.mjs tap-thinking`, output `main-tap-thinking.json`, screenshot `shots/main-tap-thinking-3-plus20s.png`. After a 150 ms tap while she was thinking about \"My name is Sam Rivera\":\n  - no reply was ever spoken; there was no /api/speech after the /api/turn;\n  - localStorage stayed `[]`;\n  - no notice appeared, only the static \"Hold the button while you talk…\" hint.\n\n**Citation errors (don't change the verdict).**\n- mary-app.tsx:184-191 on main is the controller's return block, not `onPress`.\n- hold-talk.ts:494-497 doesn't exist; main's file has 147 lines.\n- The reliability runs (s04-*, 03:10) used a local :5181 server from before f511644 (03:54). They don't reflect PR #2.\n\n**PR #2 branch: the headline case is fixed.** The branch implements most of the recommendation:\n- `onPress` only calls `voice.pauseForPress()` (`mary-app.tsx:230-238`).\n- `runner.interrupt()` moved to `onCommit`, which fires once the hold passes `MIN_HOLD_MS` (`mary-app.tsx:239`; `hold-talk.ts:187-197`).\n- A short press gives `onCancel(\"short\")`, which calls `resumeAfterTap()` and sets the `missedHold` notice (`mary-app.tsx:244-249`; `voice-line.ts:100-108`).\n- `SET_COLLECTED` now runs before the stale check (`turn-runner.ts:279-284`).\n- While she speaks, the line above the button reads \"MARY is talking · hold to cut in\" (`floor.ts:22-24`).\n- Unit test `tests/unit/hold-talk.test.ts:81-88` covers \"a mis-tap never commits\".\n\nI ran the branch on my own dev server with every route mocked (`skeptic-ux09/repro.mjs`):\n- **120 ms tap mid-welcome:** she paused about 300 ms, then resumed. The full line stayed, the notice appeared, and \"Your turn\" followed (`tap-speaking.json`, `shots/tap-speaking-plus15s.png`).\n- **150 ms tap while thinking:** she spoke both \"Nice to meet you, Sam.\" and \"What's the best email…\", and the name was saved to localStorage (`tap-thinking.json`, `shots/tap-thinking-2-after.png`).\n\n**What still fails on PR #2.**\n- **400 ms brush mid-welcome, empty transcript:** her line is cut to \"Hi, I'm MARY. I…\" and her question is never asked. The screen shows \"Your turn — hold to answer\" plus \"Didn't catch that…\" (`shots/brush-speaking-plus15s.png`).\n- **400 ms brush while thinking:** `interrupt()` aborts the /api/turn request (`turn-runner.ts:126`). No reply ever comes, the name isn't saved, and nothing re-runs the turn (`shots/brush-thinking-3-plus20s.png`). The visitor's line stays in the history, so the next turn can recover the name, and \"Your turn\" plus the notice give a next step. It is not a dead end.\n- **Minor:** after a tap, the notice says \"Didn't catch that. Hold, speak close to the phone\", although the visitor didn't speak. It also stays up after she replies (still showing at +20 s in the tap-thinking run).\n\n**Retell.** \"Changes with Retell\" is correct. `retell-call.ts:263` switches to hands-free, so the hold button isn't rendered during a Retell call.\n\nNo live AI calls were made (0 of 4). My dev server on :5309 has been stopped.",
    "better_fix": "Merge PR #2: it already fixes the tap case (pause on press, commit at MIN_HOLD_MS, a hint on a short press, collected saved before the stale check). The remaining fix is in the branch's mary-app.tsx `onMissed`, `onCancel(\"empty\")` and `onTranscribeFailed` handlers. When one of them fires after a committed hold that produced no text, either re-run the aborted turn (the visitor's last line is still unanswered), or, if she was cut off during the opener or a question, say her last question again. Pausing on commit and aborting only on `onText` would also work. Two smaller changes:\n- Give a short tap its own notice, e.g. \"Press and hold while you speak\", instead of \"Didn't catch that…\".\n- Clear `missedHold` when her next line starts, not only on the next `send()`."
  },
  "UX-11": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding is accurate for origin/main, which the live site runs. It is largely out of date for PR #2's branch (claude/iphone-audio-playback-compat-lmufp8 @783f37c). The audit's probe ran at 02:29, before the branch commits 35e5126 (\"reads emails back\"), b991dc7 (\"corrections\") and 783f37c.\n\n**origin/main (live): holds**\n- `mary-grounding.ts:298-304` accepts the email when only its local part appears in what was said.\n- I re-ran the probe against main's file (`skeptic-ux11/probe-email.out.json`). `sarah@gmail.com` from \"Sarah.\" / \"Just use my work email.\" is accepted.\n- `mary-voice.md:461` and `:511` only model \"sarah, at brightpath dot com — got it\". There is no confirmation question.\n- `live-3.log:7-8` shows MARY saying \"I've got danak at gmail dot com\" and moving straight to \"what's the business\".\n- The end screen's \"Details confirmed\" block (`end-screen.tsx:156-173`) is a static `<dl>` with no edit action.\n- The only way out is `onRestart`, which calls `newSession()` and `reload()`. That is `mary-app.tsx:531-535` on main, not 591-595 as cited.\n\n**PR #2 branch: most of it is fixed**\n- **Invented domain:** grounding now requires the domain label to have been said (`mary-grounding.ts:560-586`). The same probe returns `rejected: [\"email\"]`.\n- **Retry on rejection:** a rejected email makes the prompt ask the visitor to say it again or type it in the box (`mary-prompt.server.ts` rejectedNote).\n- **Read-back rule:** an EMAIL phase plus a rule for the turn an email arrives force a chunked, spelled read-back followed by \"Did I get that right?\" (`mary-prompt.server.ts:93`, `:118`, `:132`, `:187-188`, `:256`). The playbook has the same rule at `mary-voice.md:217-223` and `:353-355`.\n- **Correction on the end screen:** it now has an Edit action with an email-type input (`end-screen.tsx:219-230`, `:296-352`), validated by `EMAIL_SHAPE` (`lead-lifecycle.ts:412-425`).\n- **Same row on correction:** `correctLeadDetails` re-syncs through the session's existing lifecycle (`lead-lifecycle.ts:123-126`). I reproduced this with the harness against local dev on the branch, all /api routes mocked, no live calls (`skeptic-ux11/edit-email-desk.json`, `end-desk.png`, `edit-bad-desk.png`, `edit-fixed-desk.png`):\n  - The Edit email button is present.\n  - Typing \"dana at gmail\" shows \"That doesn't look like an email address.\"\n  - Saving `dana.k@gmail.com` POSTs /api/lead with the same sessionId (`w_muhw8rsw_2wexbe`), so no duplicate row.\n- **Retell prompt:** `retell/playbook-condensed.md:50` has EMAIL CHECK: read back, spell, \"Did I get that right?\", and ask them to type it after two misses.\n\n**Still true on the branch (probe and `schema.out.txt`)**\n1. The TLD is never grounded. The visitor says \"brightpath dot com\", the model proposes `sarah@brightpath.io`, and it is accepted. It is also accepted when no TLD was said at all. `readsBackEmail` checks only `local@label`.\n2. Grounding does no format check, so `sam@example` is accepted. `LeadPayloadSchema.email` is still `z.string().max(200)` (`lead-sync.ts:22`) and Retell's is `arg(200)` (`retell-shared.ts:149`). Both accept \"sam at example\".\n3. The read-back is enforced only through the prompt:\n   - `finishTurn` sets `complete = spot && (out.complete || leavesWithSpot)` (`mary-prompt.server.ts:331`) without `emailChecked`.\n   - The intent rule tells her to close at once if a visitor with name and email wants to leave.\n   - `emailChecked` only checks that MARY said the address, not that the visitor said yes.\n4. On the Retell path the end-screen Edit is switched off (`editable={!retell && …}`, `end-screen.tsx:219`, `:229`). Retell `save_lead` reuses the same `groundCollected` (`retell-lead.server.ts:238-256`), so gaps 1 and 2 carry into Retell.\n\n**Retell doc citations:** both check out in the saved copies. Echo Verification and NATO Phonetic Alphabet are off by default (`build_agent-handbook.md:75-90`, `:150-151`), and the prompt situation guide has the email-spelling pattern.\n\n**Severity:** P1 is right for the live build today. PR #2, which is about to merge, already implements most of the recommendation, so the remaining work is P2. The \"must carry into Retell\" label holds.",
    "better_fix": "Merge PR #2 to get the domain grounding, the read-back rule, the Edit action and the same-sessionId re-sync. Then close what is left:\n1. Reuse `EMAIL_SHAPE` (`lead-lifecycle.ts:412`) in three places: the email case of `groundCollected`, `LeadPayloadSchema.email` and `SaveLeadArgsSchema.email`. A malformed address then comes back as rejected, and the existing rejectedNote asks MARY to hear it again or have them type it.\n2. Ground the full domain, not just the label. Require the TLD token (\"com\", \"io\" and so on) to appear in what they said, or accept the address once MARY has read back the whole domain and they said yes. Extend `readsBackEmail` to check the TLD too.\n3. In `finishTurn`, don't allow `complete` (including the `leavesWithSpot` path) until the email has been read back and they have answered, or at least until it has been read back.\n4. On the Retell path, add a way to correct details on the end screen (it is switched off by `!retell`), for example through the existing typed-input box or a signed re-save. Optionally turn on Retell's Echo Verification."
  },
  "UX-12": {
    "refuted": false,
    "corrected_severity": "P2",
    "reasoning": "The finding holds for the live site (origin/main). On the checked-out PR #2 branch most of it is already fixed, and the finding's own evidence is wrong about that branch.\n\norigin/main (live):\n- Confirmed. `git show origin/main:src/routes/privacy.tsx` says the file does not exist, and `curl https://omnisuite.omnikom.io/privacy` returns 404.\n- The landing and footer on main have no privacy, recording or transcription wording (`git grep` over origin/main src/features, src/components and src/routes: 0 hits).\n- The Playwright harness (iPhone 13, AI and lead routes aborted, 0 live AI calls) measured 0 `<a>` links on the live landing and no match for /privacy|record|transcrib/ in the body text (scratchpad/ux-audit/skeptic-ux12/live-ios.json and live-ios.png).\n- Main's `start()` requests the mic inside the tap at mary-app.tsx:464-468, not at the 517-525 the finding cites.\n- One overstatement: live already says she is an AI. The landing subtitle, above the fold next to Start, reads \"Your AI Revenue Concierge\" (visible in live-ios.png). So the \"not told MARY is an AI\" part is weak; the recording, data-use and privacy-link parts hold.\n\nChecked-out branch (PR #2, pushed as 783f37c on origin/claude/iphone-audio-playback-compat-lmufp8; commit 344e40f, 2026-09-26):\n- The finding says a grep \"finds no user-facing hits\" in landing.tsx or site-frame.tsx. That is false here. landing.tsx:190-202 renders \"MARY is an AI. Your conversation is transcribed and saved so the Omnikom team can follow up. How your details are handled\", linking to /privacy.\n- site-frame.tsx:39-44 adds a Privacy link to `SiteFooter`.\n- src/routes/privacy.tsx is a full draft page. It covers what is collected, the Google Sheet, the AI providers via the Lovable gateway, the learning notes, the partial transcript saved when someone leaves, Retell as \"coming next\", retention and deletion.\n- The disclosure sits under both Start and Type. So the finding's core recommendation already exists and only has to merge.\n\nRemaining gaps on the branch, measured on a local dev server (landing.mjs, widths.mjs):\n- On phones the disclosure is below the fold at first paint. Measured top of the line against viewport height:\n  - iPhone 13 at 390x664: line at y=751, 664px viewport, Start button bottom at 625.\n  - iPhone SE at 375x553: 785 vs 553.\n  - Pro Max at 430x746: 772 vs 746.\n  - Pixel at 412x780: 781 vs 780.\n- It is 12px muted text. The landing section scrolls (scrollHeight 890 vs 664), so it can be reached, but a visitor who taps Start without scrolling never sees it, and the footer Privacy link (y=842) is also off-screen.\n- On desktop (1366x900) it is visible at y=732.\n- privacy.tsx:6-15 is marked DRAFT. `CONTACT` is a placeholder (\"the Omnikom team, through the details on omnikom's site\"), and retention is undecided.\n\nRetell (must_carry_into_retell holds, and Retell makes it worse):\n- retell/agent.json:15 ships `\"data_storage_setting\": \"everything\"` and sets no `data_storage_retention_days`.\n- Retell's create-agent docs confirm `everything` is the default and covers transcripts, recordings and logs. The data-retention docs confirm \"By default, data is kept indefinitely\", with retention adjustable from 1 to 730 days.\n- The branch privacy page says \"we do not keep the audio\". That is true for MARY's path, where the audio only goes to /api/transcribe, but it becomes false under Retell's `everything`.\n- docs/architecture/retell-migration.md:231-233 already lists privacy and disclosure as an open operator question.\n- Retell's ToS (last updated 2026-06-01) is correctly quoted: section 4.2 covers notifying callers of recording and disclosing AI voice use \"at the beginning of the call\", and Addendum 1 §10(b) says \"before or at the start of the call\".\n- The training claim is only partly supported. ToS section 11 grants Retell use of call data for model training (\"de-identified and aggregated\") but names no opt-out there, so \"unless opted out\" is unverified.\n- MARY's spoken openers (src/lib/mary.functions.ts:40-52) say \"I'm MARY\" but never \"AI\", and Retell's `begin_message` is `{{opening_line}}` built from the same `OPENERS` (retell/llm.json:3, retell-handlers.server.ts:300). So \"keep the AI mention in her opener\" has nothing to keep: there is no spoken AI disclosure at the start of the call.\n- I did not re-check the EU AI Act, US state or ElevenLabs, Sesame and Vapi citations.\n\nSeverity: P1 is fair for the live site today (email and phone collected, full transcripts to a sheet, no notice or privacy page). But the fix is committed on PR #2, which is about to merge, and live already calls her an AI. The work still left once PR #2 merges is:\n- getting the disclosure above the fold on phones,\n- filling in the contact and retention,\n- adding a spoken AI line to the openers,\n- choosing Retell's storage and retention settings and updating the privacy page to match.\nThat is P2 work, not a missing feature.\n\nThe repo was not modified. The dev server was started only for measurement and stopped afterwards.",
    "better_fix": "Merge PR #2's disclosure and privacy page instead of re-implementing them. Then do these, in order:\n1. Move the one-line disclosure directly under, or inside, the Start/Type button row, so it is on screen at 390x664 and 375x553. One option is to replace the \"Early access… About three minutes\" paragraph's position with the disclosure, or to shorten both into one line above the buttons. Re-measure with the harness so the line's top is below the viewport height on every phone size.\n2. Fill in `CONTACT` (a real email) and a concrete retention period in src/routes/privacy.tsx.\n3. Add \"an AI\" to each `OPENERS` entry in src/lib/mary.functions.ts, e.g. \"Hi, I'm MARY, Omnikom's AI assistant…\". This one line covers the MARY voice path and Retell's `begin_message`. Watch the retell-config playbook-anchor test.\n4. Before Retell goes live:\n   - Set `data_storage_retention_days` in retell/agent.json, e.g. 90.\n   - Decide between `everything` and `everything_except_pii`, given that grounding needs transcripts.\n   - Change the landing line to \"recorded and transcribed\" if recordings are kept, and update privacy.tsx's \"we do not keep the audio\" and \"Where it goes\" sections.\n   - Ask Retell in writing whether training use under ToS §11 can be opted out of, because the ToS itself names no opt-out."
  }
}
